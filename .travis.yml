language: python

python:
  - 3.7

#env:
#  global:
#    - LINTLY_API_KEY=${GITHUB_TOKEN}

install:
  - pip install -r requirements.txt

before_script:
  - flask db migrate
  - flask db upgrade

script:
  - flake8 .
  - coverage run -m pytest
